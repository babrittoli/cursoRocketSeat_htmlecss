// Operadores logicos (logical operators)

// -2 valores booleanos, quando verificados, 
// resultará verdadeiro ou falso

let pao = true
let queijo = true

// AND && 
console.log(pao && queijo) // tem que ter os dois
// OR ||
console.log(pao || queijo)  // tem que ter um ou outro pelo menos

// NOT !
(console.log(!pao)) // negação troca o valor 